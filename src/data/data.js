class Data {
    async constructor(file){
        this._file = file;
        fetch('/data/comets.json')
        this._data = dataFile;
    }

    get data() { return this._data }
}

let data;

const load = () => {
    return 
}

export load;
